export const interviewAnswerByQuestionEn: Record<string, string> = {
  'Qual é a diferença entre Deployment, StatefulSet e DaemonSet?': 'Deployment manages interchangeable replicas and rolling updates for usually stateless workloads. StatefulSet adds stable identity, ordered lifecycle, and stable storage associations. DaemonSet ensures one Pod runs on every selected node, which is common for node-local agents.',
  'O que acontece quando uma readiness probe falha?': 'The Pod becomes NotReady and its endpoint is normally excluded from Service traffic. A readiness failure does not by itself restart the container; restarts are associated with liveness failures when a liveness probe is configured.',
  'Como investigar um Service sem endpoints?': 'Start with the Service selector and Pod labels, then verify namespace and Pod readiness. Inspect EndpointSlices next. Only after confirming endpoints should you move down to DNS, kube-proxy/CNI, NetworkPolicy, or lower-level network troubleshooting.',
  'Como o quorum do etcd afeta a disponibilidade do cluster?': 'etcd requires a majority to make progress. A three-member cluster requires two members, so it tolerates one member failure but loses quorum after two failures. Because Kubernetes persists cluster state in etcd, loss of quorum directly affects control-plane writes and reconciliation.',

  'Qual é a diferença entre imagem e container?': 'An image is an immutable layered artifact used as a template. A container is a runtime instance of that image with a process, namespaces, and a writable layer. Multiple containers can be created from the same image digest.',
  'Como funciona um multi-stage build?': 'A Dockerfile can use multiple FROM stages. A builder stage contains compilers and development dependencies, while the runtime stage copies only the required artifacts with COPY --from. This usually reduces final image size and attack surface.',
  'Por que um container não é equivalente a uma máquina virtual?': 'A virtual machine normally runs its own guest operating system and kernel on a hypervisor. A container isolates processes using the host kernel through namespaces, cgroups, capabilities, and related mechanisms, so it does not boot a separate guest kernel.',

  'O que é um inode e como pode causar filesystem cheio?': 'An inode stores filesystem object metadata. A filesystem has a finite inode supply, so millions of small files can exhaust inodes even when byte capacity remains. Check df -i, locate file-heavy directories, and remove or consolidate files rather than only looking at df -h.',
  'Qual é a diferença entre processo e thread?': 'A process has its own process identity and virtual address space, while threads within a process share much of that address space and resources. Threads have independent execution contexts and scheduling state but are more tightly coupled than separate processes.',
  'Como investigar alta carga com CPU aparentemente baixa?': 'Inspect runnable and uninterruptible tasks rather than CPU percentage alone. Use vmstat, ps state fields, iostat, pidstat, and /proc to find D-state tasks, blocked I/O, memory pressure, or storage/network waits that contribute to load average.',

  'Qual é a diferença entre TCP e UDP?': 'TCP is connection-oriented and provides ordered, reliable byte-stream delivery with retransmission and flow/congestion control. UDP is datagram-oriented with lower transport overhead and no built-in delivery or ordering guarantee; applications add reliability when they need it.',
  'Como investigar perda de pacotes intermitente?': 'Follow the packet path and collect evidence at multiple points. Check interface errors, routes, ARP/neighbor state, MTU, firewalls, congestion, and packet captures near source and destination. Correlate the loss with time, path changes, and device metrics before changing configuration.',
  'Como NAT afeta comunicação fim a fim?': 'NAT rewrites addresses and sometimes ports, so the original endpoint identity may not be visible end to end. Stateful NAT also requires return traffic to traverse the translating device. This affects troubleshooting, inbound reachability, protocols that embed addresses, and some security assumptions.',
  'Como funciona uma VPN site-to-site e o que precisa estar correto para duas redes se comunicarem?': 'A site-to-site VPN creates encrypted tunnels between network gateways, commonly with IPsec/IKE. The tunnel being up is only one requirement: non-overlapping prefixes, forward and return routes, firewall rules, NAT behavior, MTU/MSS, and DNS must also be correct.',
  'Como você faria duas redes diferentes se comunicarem?': 'First ensure the address spaces do not overlap. Select a Layer 3 connectivity mechanism such as routing, peering, a transit router, MPLS, or VPN; install routes in both directions; permit the required traffic; then validate NAT, MTU, DNS, and the application path.',
  'Quais portas e protocolos de rede você considera essenciais conhecer em uma entrevista?': 'Know common services such as SSH 22/TCP, DNS 53/UDP+TCP, DHCP 67/68 UDP, HTTP 80/TCP, HTTPS 443/TCP, NTP 123/UDP, LDAP 389, LDAPS 636, SMB 445, RDP 3389, IKE 500/UDP, and IPsec NAT-T 4500/UDP. Also know that ICMP, ESP, and GRE are IP protocols rather than TCP/UDP ports.',

  'Qual é a diferença entre OAuth 2.0 e OpenID Connect?': 'OAuth 2.0 is an authorization framework for delegated access. OpenID Connect is an identity layer built on OAuth 2.0 that adds authentication semantics, an ID Token, and standardized user identity claims.',
  'Como funciona uma cadeia de confiança de certificados?': 'The client validates the leaf certificate against one or more intermediate CAs until it reaches a trusted root, while also checking signatures, validity, hostname, and relevant constraints. A valid chain does not by itself guarantee that every application-level security decision is correct.',
  'O que significa Zero Trust na prática?': 'Zero Trust means access is not trusted merely because of network location. Authenticate and authorize each request based on identity and context, apply least privilege, segment resources, continuously evaluate signals, and assume that compromise can occur inside the network.',

  'Qual é a diferença entre chart e release?': 'A Helm chart is a versioned package containing templates, metadata, and default values. A release is one installed instance of a chart with a specific configuration and revision history in a Kubernetes cluster.',
  'Como validar um chart antes do deploy?': 'Use helm lint, render with helm template using the target values, validate the resulting Kubernetes manifests, and test upgrades in a representative environment. JSON schema for values can catch type/configuration errors before rendering.',
  'Como usar Helm com Argo CD?': 'Keep chart source and environment configuration in Git and let Argo CD render and reconcile the desired manifests. Avoid routine manual helm upgrade operations against a GitOps-managed release because the reconciler will treat them as drift.',
  'Como tratar secrets e values por ambiente em Helm sem duplicar charts?': 'Keep one reusable chart, defaults in values.yaml, and small environment-specific override files. Keep plaintext secrets out of Git; integrate a secret-management pattern such as External Secrets, SOPS, or another platform-approved mechanism.',
  'Como diagnosticar um helm upgrade que falha ou deixa a release em estado inconsistente?': 'Separate rendering failures from Kubernetes API failures and workload runtime failures. Use helm template --debug, helm status/history/get manifest, then inspect Kubernetes events and resource state. In GitOps, also check desired-versus-live reconciliation before manual changes.',

  'Por que o Terraform precisa de state?': 'Terraform state maps configuration addresses to real remote objects and stores metadata needed to plan changes. Without that mapping Terraform cannot reliably know which existing object corresponds to each resource block or how to calculate safe updates.',
  'Como evitar aplicação concorrente sobre o mesmo state?': 'Use a remote backend with supported state locking, keep one authoritative state location, and serialize apply operations through CI or another controlled workflow. Do not copy local state between operators or bypass a lock without understanding whether another operation is still active.',
  'Qual é a diferença entre module, resource e data source?': 'A resource declares an object Terraform manages through its lifecycle. A data source reads information without owning that object. A module groups resources, data sources, variables, and outputs into a reusable configuration unit.',
  'Qual é a diferença entre resource, data source, module e provider no Terraform?': 'A provider implements the API integration. A resource declares an object Terraform manages, a data source reads external information without owning it, and a module composes reusable Terraform configuration behind inputs and outputs.',
  'Quando usar Terraform workspaces e quando evitar workspaces?': 'Workspaces are useful when the same root configuration needs multiple largely identical state instances. Avoid using them as the only isolation mechanism when environments differ in accounts, credentials, backends, policy, topology, ownership, or lifecycle; separate root modules and state boundaries are clearer then.',
  'Como investigar e recuperar um state file inconsistente ou perdido?': 'Stop concurrent applies and preserve backend versions or backups first. Compare configuration, state, and real infrastructure with plan. Use import for existing resources missing from state, refresh-only to intentionally adopt external changes, and moved blocks for address refactoring. Run plan again before any apply.',
  'Como corrigir drift sem destruir recursos?': 'First decide which state is correct. If code remains authoritative, review the plan and reconcile infrastructure toward it. If an external change is legitimate, update HCL and intentionally refresh or import state. Never treat drift as an automatic reason to overwrite production.',

  'O que significa idempotência no Ansible?': 'Idempotency means repeated runs converge to the same desired state and should report no change when the target is already correct. Prefer idempotent modules and explicitly control changed_when when shell or command execution is unavoidable.',
  'Qual é a diferença entre task, handler e role?': 'A task is one unit of work. A handler is a task normally triggered by notify when another task changes something, commonly for service restarts. A role packages tasks, handlers, templates, defaults, variables, and files into a reusable component.',
  'Quando usar Terraform e quando usar Ansible?': 'Terraform is strongest for declarative infrastructure lifecycle and dependency graphs; Ansible is strongest for configuration management and orchestration inside or across hosts and services. They often complement each other rather than being direct substitutes.',
  'Como a idempotência funciona no Ansible e como diagnosticar uma task que sempre retorna changed?': 'Run the play twice and inspect the task that still reports changed. Look for shell/command without guards, incorrect changed_when, nondeterministic templates, timestamps, ownership or mode changes, and a module that does not model the desired state idempotently.',
  'Como organizar roles, handlers e variable precedence sem criar configuração imprevisível?': 'Keep roles cohesive, put overrideable defaults in defaults, use handlers only for actions triggered by actual changes, and minimize variable sources. Inspect effective host variables with ansible-inventory and document where environment-specific overrides belong.',
  'Como fazer rollout seguro em centenas de hosts?': 'Limit blast radius with canaries and serial batches, validate health between batches, define appropriate failure thresholds, and stop progression when validation fails. Use check/diff modes where meaningful and keep rollback or remediation steps ready.',
  'Como investigar um playbook que funciona em um host e falha em outro?': 'Compare effective inventory variables, facts, OS/package versions, interpreter discovery, connectivity, user/become behavior, and dependencies. Reproduce with --limit on the failing host and -vvv, then identify the first environmental difference.',

  'Qual é a diferença entre continuous delivery e continuous deployment?': 'Continuous delivery keeps software in a deployable state and may retain a manual production gate. Continuous deployment automatically releases every change that passes the required automated controls.',
  'Como o Argo CD detecta e corrige drift?': 'Argo CD renders desired state from its configured source and compares it with live Kubernetes resources. Differences make the Application OutOfSync. With self-heal enabled, Argo CD can reapply desired state; prune can remove resources no longer present in the desired configuration.',
  'Por que usar tags de imagem imutáveis?': 'Immutable tags or, preferably, digests bind a deployment to a specific artifact. That improves traceability, repeatability, incident analysis, and rollback because the same reference cannot silently point to different image content.',
  'Qual é a diferença entre Continuous Delivery, Continuous Deployment e GitOps?': 'Continuous Delivery focuses on keeping changes releasable, Continuous Deployment automatically promotes passing changes to production, and GitOps defines desired operational state in Git and uses a reconciler to converge the runtime environment toward that state.',
  'Por que build once, promote many é preferível a recompilar por ambiente?': 'Promoting the same immutable artifact reduces supply-chain ambiguity. Development, staging, and production evaluate the same bytes while environment differences remain configuration, making validation and rollback much more trustworthy.',
  'Como Argo CD detecta drift e quando usar self-heal e prune?': 'Argo CD compares rendered desired state with live cluster resources. Self-heal corrects out-of-band changes; prune removes objects no longer declared. Enable both deliberately because they can revert manual hotfixes or delete resources that were unintentionally removed from desired state.',
  'Como reduzir o risco de secrets de longa duração em pipelines?': 'Prefer workload identity or OIDC federation so jobs exchange short-lived identity tokens for temporary provider credentials. Restrict subject/audience, token permissions, environment approvals, and scope any remaining secrets to the minimum required.',

  'Qual é a diferença entre Security Group e Network ACL?': 'Security Groups are stateful controls attached to resources/ENIs, so return traffic for an allowed flow is automatically recognized. Network ACLs are stateless subnet-level rules, so ingress and egress must both be permitted explicitly.',
  'Como projetar uma aplicação altamente disponível em duas AZs?': 'Distribute load-balanced application capacity across at least two Availability Zones, use health checks and automated replacement, keep state in resilient managed services such as Multi-AZ databases where appropriate, and remove single-AZ dependencies from the request path.',
  'Quando usar SQS em vez de comunicação síncrona?': 'Use SQS when producer and consumer do not need to complete in the same request, when you need buffering, independent scaling, retry handling, or resilience to temporary consumer outages. Consumers should be designed for duplicate delivery and idempotency.',
  'Onde colocar uma IAM role para uma aplicação acessar um S3 bucket?': 'Attach the role to the workload runtime: EC2 through an instance profile, Lambda through its execution role, ECS through taskRoleArn, and EKS through Pod Identity or IRSA. Put the least-privilege S3 actions on that workload role; do not distribute static access keys.',
  'Qual é a diferença entre IAM role, IAM policy e bucket policy no S3?': 'An IAM role is an assumable identity. Identity policies attached to that role define what it can do. An S3 bucket policy is a resource-based policy attached to the bucket and can grant or deny access to principals, including cross-account principals, with resource-specific conditions.',
  'Qual é a diferença entre RDS Multi-AZ e Read Replica?': 'Multi-AZ primarily provides availability and failover by maintaining a standby or highly available deployment across Availability Zones. Read Replicas primarily provide read scaling and can have different replication and promotion characteristics depending on the engine.',
  'Como uma Lambda em subnet privada acessa RDS, S3 e a Internet?': 'RDS is reached through private VPC routing and Security Groups. S3 can be reached privately through an S3 Gateway Endpoint. Internet egress for IPv4 normally requires a route from the Lambda-connected private subnet through a NAT Gateway and then an Internet Gateway.',
  'Qual é a diferença entre ECS task role e task execution role?': 'The task role is assumed by application containers and grants business/API permissions such as S3 or SQS access. The task execution role is used by ECS/Fargate for platform operations such as pulling images from ECR and sending logs where configured.',
  'Como conceder acesso a S3 para um Pod no EKS sem usar credenciais estáticas?': 'Use EKS Pod Identity or IRSA so the workload/ServiceAccount receives temporary credentials for a dedicated IAM role. Grant that role only the required S3 actions. Avoid putting application S3 permissions on the node role.',
  'Quando usar SNS, SQS ou SNS mais SQS?': 'Use SQS for durable queueing, buffering, retries, and pull-based workers. Use SNS for pub/sub fan-out. Combine SNS and SQS when multiple consumers need independent durable queues and can process at different rates.',
  'Como conectar uma VPC AWS à rede on-premises?': 'Choose Site-to-Site VPN for encrypted connectivity over the Internet, Direct Connect for dedicated connectivity, or combine them for specific resilience requirements. Transit Gateway can centralize multiple VPC and hybrid attachments. In every case, design non-overlapping CIDRs, forward/return routes, security policy, DNS, and redundancy.',
  'Como funciona AWS Site-to-Site VPN e por que existem dois túneis?': 'AWS Site-to-Site VPN establishes IPsec connectivity between a customer gateway and a Virtual Private Gateway or Transit Gateway. AWS provides two tunnels so the customer device can maintain connectivity during tunnel maintenance or failure; configure both whenever the customer equipment supports it.',
  'Quando usar VPC Peering, Transit Gateway, VPN ou Direct Connect?': 'Use VPC Peering for a limited number of direct non-transitive VPC connections, Transit Gateway for hub-style routing across many VPCs and hybrid attachments, Site-to-Site VPN for encrypted network connectivity over the Internet, and Direct Connect when dedicated private connectivity is required.',
};
