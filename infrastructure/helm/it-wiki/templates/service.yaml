apiVersion: v1
kind: Service
metadata:
  name: {{ include "it-wiki.fullname" . }}
  labels:
    {{- include "it-wiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "it-wiki.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
